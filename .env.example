# Copy to .env and fill in. NEVER commit .env.
OPENAI_API_KEY=sk-proj-...

# optional
GCODE_MODEL=gpt-5
GCODE_USER=rishii            # who to attribute spend to in the usage log
GCODE_BUDGET=0              # USD cap per turn, 0 = no cap
